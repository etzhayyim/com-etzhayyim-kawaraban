# kawaraban — sovereign actor repo
# PRIVATE fulltext analysis buffer (G4 internal, ADR-2607010930) — never committed
/data/ingest/fulltext-buffer/
/.cpcache/
/.clj-kondo/.cache/
# ADR-2607110200: kawaraban's own Ed25519 identity — NEVER commit a private key.
/.kawaraban/
